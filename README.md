# Docker Composed Rails kit (with NGINX, Postgres and Redis)

## Getting Started

1. Install Docker and Docker Compose
1. Run `./setup.sh`
1. Run `docker-compose up` or `start`

Go to `http://localhost/`
